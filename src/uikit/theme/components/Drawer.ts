import { colors } from "../colors";

const Drawer = {
  baseStyle: {
    overlay: {
      bg: `${colors.purple[900]}D9`,
      backdropFilter: "blur(8px)",
    },
    dialog: {
      bg: "ui-bg",
    },
    dialogContainer: {},
  },
};

export default Drawer;
